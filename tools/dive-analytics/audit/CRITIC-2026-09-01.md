# Dashboard critic — 2026-09-01

Model: claude-fable-5 · prompt: critic-prompt.md · artifact: data.json generated 2026-09-01T00:00:44.804Z

---

## 1. Cognitive load
- PASS — Above the fold: gauge (45), six worded check states, hero total (7,002), three trend bars plus one verdict percentage — roughly seven numbers, inside the 12-number budget, one question per card.
- WARN — The hero gives a promo-inflated 7,002 the same unqualified visual weight as any honest total; the only qualifier slot (`heroMarker`) covers plays coverage, not the outlier condition (see lens 4).

## 2. Readability
- PASS — No hard-flag jargon at glance or click layer; bullets ("13% of X announce impressions became plays… well under earlier episodes as they stand now") and drivers are plain and digit-free where required.

## 3. Verbosity / suppression
- WARN — The pace-rank insight restates "7,002 total views" already carried by the hero directly above it; the pace figure alone carries the claim (restates-visible-number rule).

## 4. Fact-check
- FAIL — The pace-rank insight contradicts the shipped data: `episodes[7].anomaly` says "treat as promo-driven outlier, not topic signal," yet the insight recommendation says "This topic/format is landing… repeat that on the next episode." The 212%/#1-of-4 arithmetic matches `baselines.pace`, but the interpretation reverses the stored honesty note.
- FAIL — Condition without marker: E7 (`outlier.flagged: true, provisional: true`) and E3 (flagged) surface no outlier marker anywhere in the page source — hero, strip, panel, and table all render their totals unmarked. No code path reads the anomaly/outlier flag for display; only About describes the concept in the abstract. Every flagged condition in the data must surface a marker.
- PASS — Re-derived arithmetic checks out: X plays sum 15,283 vs YT 15,092 (50.3% ✓); E1 subs/1k 8.8 blended and 13.2 DesignerTom ✓; Dive Club 4.3 vs 1.1 (E2) and 5.7 vs 0 (E4) ✓; E3/E7 outlier values exceed 2× their stamped `units` typicals ✓; livePull typicals 71 peak / 167 chat match the medians of the stamped 5-episode window ✓.
- PASS — Show-health discipline holds: score 45 is 2.8 points off `weightedMean` 47.8 (within 8); every pros/cons bullet carries exactly one number matching its fact's display; unavailable checks are null with reasons and zero weight; `ageDays: 1` correctly surfaces "health read is behind" in the stamp; episodes under 21 days (E5–E7) show nothing score-like, and E1–E4's null scores render only their stored reasons at click layer.
- WARN — Verify by hand: the saved `commentRate` fact displays 0.2 while the served E7 `commentersPer1k` is 0.1 — likely save-date drift covered by the "behind" stamp, but check the sentiment tooltip doesn't show 0.2 beside a panel showing 0.1.
- WARN — Verify by hand: discovery-surfaces insight says 64.3% of YouTube views (8,774) come from subscribers, implying a ~13.6k base, but total YT views shown is 15,092 (8,774 would be 58.1%). Traffic data isn't in this export; confirm which base the 64.3% was computed on.

## 5. Decision usefulness
- PASS — Compounding: yes in ~10s — the growth-trend card's three bars plus "newest first week came in 32% under the one before" answer it honestly.
- PASS — Where to spend effort: mostly yes — Reach reads "fragile" at glance, and the distribution insights (X is half of watching, discovery surfaces near-zero, Dive Club converts) give concrete direction one scroll down.
- FAIL — Latest episode vs the rest: the glance answer ("7,002, pacing #1, 212% ahead") is exactly wrong for a decision — the data itself says this is a promo-driven outlier, and that fact reaches the reader nowhere.

## Verdict
The machinery is in excellent shape: arithmetic re-derives cleanly, absence never renders as zero, young episodes show no scores, the health score sits within tolerance of its weighted mean, and the behind-stamp fires correctly. But the page's single loudest story — the newest episode's blowout numbers — is shipped without the one caveat the data explicitly stores. E7 is a flagged provisional promo outlier, and the hero, strip, table, and the pace insight all present its numbers as organic momentum, with the insight going as far as telling the owners to repeat the "topic/format." That is the one place a reader would act on this dashboard this week, and it would steer them to the wrong conclusion.

## The one recommendation
Render the stored outlier condition: when `outlier.flagged` is true, put an amber "promo-driven — read with care" marker on the hero, the episode card, and the panel (provisional wording while under 21 days), and regenerate or suppress the pace-rank insight so its recommendation agrees with the stored anomaly note instead of contradicting it.
