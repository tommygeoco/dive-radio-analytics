# Dashboard critic — 2026-09-01

Model: claude-fable-5 · prompt: critic-prompt.md · artifact: data.json generated 2026-09-01T01:04:33.888Z

---

## 1. Cognitive load
- WARN — Above the fold sits at the 12-number cap: gauge 48, "50 is usual", hero 7,008, seven carousel totals, plus "2%" injected into Today's-read do-next ("check whether the 2% mark hold improves") — a glance-layer number needing more than 5 words of explanation.
- PASS — Surfaces map cleanly to the three questions: health row = one, growth-trend card = one, hero/carousel = one; the check rows stay word-only.

## 2. Readability
- WARN — "Dive Club also held viewers longer on E2, with 12 share watched against DesignerTom's 9.8" — no unit; "12 share watched" is not parseable by a cold reader. Should read "12% of the video watched".

## 3. Verbosity / suppression
- FAIL — "lean-into-dive-club" claims "On finished episodes Dive Club out-converts DesignerTom" from n=2 (E2, E4), while E1 — cited in a neighboring insight — shows the opposite (DesignerTom 13.2 vs Dive Club 5.6 per 1k). An n<3 claim dressed as a pattern, contradicted on the same page.
- WARN — "rebuild-live-momentum" says "peak live viewers fell from 90 on E1 to 53 on E3" but peak has since recovered: 63, 64, 71, 72 — and the health card scores peak 72 vs typical 71 (≈, inside the ±5% quiet zone). Accurate numbers, stale framing presented as current signal.

## 4. Fact-check
- FAIL — "pace-rank" insight: "This topic/format is landing… repeat that on the next episode" for E7 (7,008 views, 212% ahead, verified: 4731/1514 = 3.12). But E7's own stored anomaly reads "more than double what a typical episode gets… treat as promo-driven outlier, not topic signal" (outlier.flagged: true, provisional: true). The insight converts a promo spike into topic advice — exactly what the anomaly text forbids.
- FAIL — No render path exists for the outlier condition: the page source never reads `e.anomaly` or `e.outlier.flagged` (the string "anomaly" appears only in `catKeyFor` id-matching). E3 and E7 carry a stored honesty condition that surfaces no marker anywhere — the hero shows E7's 7,008 with no flag.
- FAIL — "lean-into-dive-club": "12 share watched against DesignerTom's 9.8" — stored E2 designertom `averageViewPercentage` is 9.73 in all three history pulls, which rounds to 9.7, not 9.8.
- WARN — Verify by hand: the render code reads `DATA.baselines.pace[slug]`, `DATA.baselines.watchPctBySlug`, and `DATA.baselines.typicalCurve.points`; the provided export has pace only under `episodes[].pace`, `watchPct` (not bySlug), and a typicalCurve with no `points`. If shipped data.js matches this export, the Same-age pace row, the Pace table column, watched-vs-typical arrows, and the dashed typical curve all silently never render despite valid data (E5 −37%, E6 −27%, E7 +212%).
- PASS — Health arithmetic re-derives cleanly: score 48 within 8 of weightedMean 49.8; announceToPlay 13% (1157/8916) vs typical 31.5% (median of 33.4/29.6/40.8/23.3); xShare 45.8% vs 47.8%; live typicals 71 and 167 match window medians; "convert-the-x-audience" sums verified (15,289 X plays, 15,093 YT views, 50.3%); "restore-e1-subscribe" per-channel figures all match historyLines; each pro/con bullet carries exactly one number matching its cited fact's display value, with the required "X replies are missing" phrase present.
- PASS — Honesty markers hold: E5–E7 (under 21 days) render nothing score-like; E1–E4's null scores carry stored reasons; week1 exclusions and E7's "pending: under 7 days" surface; commentersPer1k absent with its note; stamp honest at ageDays 0; checkSetChange note renders for audienceQuality joining; trend has the required 7 points.

## 5. Decision usefulness
- WARN — Is it compounding? Yes-ish in 10s: growth-trend verdict is honest ("newest first week 32% under the one before"), though only 3 clean first weeks exist and the card says so.
- WARN — Where should marketing go? Partial: "Reach: Fragile" and the distribution insights answer it, but only below the fold and one click deep.
- FAIL — How did the latest episode do? A cold reader sees 7,008 views, #1 of 4, "topic is landing" — and never learns the stored data calls it a promo-driven outlier. The glance answer is wrong.

## Verdict
The numbers on this page are almost all right — I re-derived the health score, the reach ratios, the platform sums, and the per-channel subscriber figures and they check out, and the wait-states and absence handling are exemplary. But the single most important fact in the data this week — that the star episode is a flagged promo outlier — never reaches the page. No code renders the anomaly, and the pace insight actively converts the promo spike into "repeat this topic" advice, contradicting the data's own warning. Add one small number error (9.8 vs 9.73), one two-episode claim dressed as a pattern, and a probable set of dead data paths that silently drop pace and typical-curve rendering, and the shipped state is honest in its plumbing but misleading in its headline story.

## The one recommendation
Surface the stored promo-outlier flag wherever E7's numbers appear — an amber marker on the hero and carousel card reading "promo-driven spike" from `e.anomaly` — and gate the pace-rank insight so a flagged episode's pace is stated as promo effect, never as topic signal.
