# Dashboard critic — 2026-08-24

Model: claude-fable-5 · prompt: critic-prompt.md · artifact: data.json generated 2026-08-24T19:00:45.541Z

---

## 1. Cognitive load
- PASS — Above the fold a cold reader meets ~8 numbers: the gauge (51), the hero total (2,133), and six carousel totals; the six diagnosis rows carry words (Healthy/Steady/Fragile/Not in yet), and the growth card is a wordless "not ready" sketch. Within the 12-number budget, one question per surface.

## 2. Readability
- FAIL — "judge the change by the middle-half hold afterward" renders in the glance-layer Today's read card (do-next action #1, pasted verbatim from the insight recommendation). "Middle-half hold" fails the plain-words test for a reader who never read a spec.
- PASS — Everything else is plain: "50 is this show's usual level," "Not in yet," "compared with earlier episodes as they stand now" — no median/delta/composite/velocity anywhere in glance or click copy.

## 3. Verbosity / suppression
- WARN — The six episode totals appear twice in one screen: on the carousel cards ("2.1k", "3.2k"…) and again as the chart's bar-end labels — a restatement of visible numbers (contract rule: suppress numbers that restate other visible numbers). The Today's read card also pastes two full multi-sentence recommendations where one sentence each would fit the layer.
- PASS — Suppression discipline is exemplary: pace absent below 3 peers, growth trend withheld at 2 clean first weeks with the reason stated, health trend null at 3 saved days, all four `insightsStale` entries correctly off the page.

## 4. Fact-check
- PASS — Reach numbers re-derive: 23% = 2,064/8,982 for E5; typical 34.4% = median of {34.4, 29.4, 41.1} from the stamped window E1/E2/E4; xShare 64.1% = 2,064/3,218 vs typical 46.6%. All mature-stamped and the con bullet honestly says "as they stand now."
- PASS — livePull typicals re-derive age-free from the stamped window E1/E2/E4/E5 with E3 excluded as promo outlier: peak 67.5 = mid(64,71) of {63,64,71,90}; chat 238.5 = mid(167,310). E3's anomaly numbers (4,206 > 2×1,801; 19,653 > 2×6,116) check out against `outlier.units`.
- PASS — Governance holds: score 51 is within 8 of weightedMean 51.9; `ageDays` 0 so no "behind" stamp; each bullet carries exactly one number matching its fact's display (71, 6, 127, 23%); insight arithmetic re-derives (8.9/2.9/0 per-1k match stored conversion checks; 13.3 = 7/526×1k, 5.7 = 4/704×1k, and E1 is indeed the only episode where DesignerTom converts better).
- WARN — verify by hand: the pros bullet says "6 recent comments were clearly positive," but `enjoyCount` across the last three episodes sums to 9 (2+1+6); the fact's source (`comments.list`) isn't in this export, so confirm the gap is mixed/held comments, not a bad count.
- WARN — verify by hand: no template in index.html reads `episode.anomaly`, so E3's stored outlier sentence (and its `provisional: true` flag) may surface nowhere on E3's own surfaces — only in About and exclusion reasons. Confirm the promo-outlier marker renders on the E3 card or panel.

## 5. Decision usefulness
- PASS — "Is the show compounding?" — the honest answer is "can't tell yet," and the glance layer says exactly that in one sentence with the unlock condition ("three clean first weeks"). ~10 seconds.
- PASS — "Where should marketing go?" — the cons bullet (23% announce→play, below typical) plus the hero's YouTube/X split answer it at glance.
- PASS — "Latest episode vs the rest?" — hero total + carousel row + chart bars answer views immediately; E6's health is honestly pending, not faked.

## Verdict
This is a disciplined ship. Every number I could re-derive checked out — reach ratios, live typicals, outlier multiples, subscriber math from raw history — and the suppression machinery is doing its job everywhere: no score under three weeks, no trend under seven days, no pace under three peers, stale insights held off the page. The two real blemishes are cosmetic-but-visible: one jargon phrase pasted into the glance layer, and six totals printed twice on one screen. Two verify-by-hand items (the "6 positive" count and whether E3's outlier marker renders at all) should be closed before the next audit but block nothing today.

## The one recommendation
In the Today's read card, stop pasting full insight recommendations verbatim: render one plain trimmed sentence per action (dropping phrases like "middle-half hold" from the glance layer), with the full recommendation staying one click away in the insight list where it already lives.
