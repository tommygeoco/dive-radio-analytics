# Dashboard critic — 2026-08-24

Model: claude-fable-5 · prompt: critic-prompt.md · artifact: data.json generated 2026-08-24T17:46:01.997Z

---

## 1. Cognitive load
- PASS — Above the fold a cold reader faces roughly ten numbers: the gauge (51), the hero total (2,133), and six strip totals; the diagnosis rows are word-states, the growth card is a number-free skeleton, and each surface answers exactly one of the three questions.

## 2. Readability
- WARN — "judge the change by the middle-half hold afterward" (front-load-demos recommendation) fails the plain-words test: "middle-half hold" is unparseable without reading a spec. The mature-basis phrase "as they stand now, not at the same age" elsewhere is a model of the plain alternative.

## 3. Verbosity / suppression
- PASS — Small-n suppression holds everywhere it's tested: growth trend shows an honest skeleton at 2 clean first weeks, same-age pace renders nothing (every rank is null), and the typical watch curve is absent at n=2.
- WARN — Pro bullet "peaked at 71 live viewers, a touch above what earlier shows usually reach": 71 vs typical 67.5 is +5.2%, which the page's own quiet-zone badge (rounds to 5%, ≤ QUIET_ZONE) would render as "≈ noise" — yet it's shipped as a Helping signal.

## 4. Fact-check
- PASS — Core arithmetic re-derives exactly: score 51 is within 8 of weightedMean 51.9; all four bullets carry one number each matching their fact's display (71, 6, 127, 23%) with the required "X replies are missing" phrase present; 12,791 X plays vs 9,875 YT views = 56.4%; announce typical 34.4% = median of {34.4, 29.5, 41.1}; chat typical 238.5 = median of {383, 310, 167, 100} with the promo outlier correctly excluded; E1's 13.3 vs 5.7 subs per 1,000 checks out from the history lines.
- WARN — "lead-with-dive-club" cites "5,144 views vs 3,707"; today's history lines sum to 5,152 vs 3,716. The insight matches the 08-23 pull, one day behind the served data. Drift is <0.2% — inside noise, and insightsStale being empty is consistent — but the insight should regenerate with the next build.
- WARN — verify by hand: the page reads `DATA.baselines.pace[slug]` and `DATA.baselines.watchPctBySlug`, but this export carries pace on each episode and only `baselines.watchPct` with no per-slug map. Nothing differs visibly today (every pace rank is null, watchPct typical is null at n=2), but confirm the served data.js exposes those paths before pace ever has a rank to show.
- WARN — Live trend verdict basis: `newestVsPrevious.live.pct` = 11, but the bars beside it chart average live viewers (50 vs 46 = +9%); 11% matches peak (71 vs 64). The stored value is canonical and I won't recompute it, but the verdict sentence can't be re-derived from the numbers on the same card — the basis needs stating.
- WARN — E3 is a flagged promo outlier ("4,206 X plays vs a typical 1,801") and posts the show's largest total (5,811) — the longest bar in the default standings — with no outlier mark on the chart, strip, or hero; only About explains it. Keeping it in observed totals is constitutional; leaving it unmarked lets it read as topic signal.

## 5. Decision usefulness
- PASS — Q1 (compounding): the glance answer is honestly "can't tell yet" — the skeleton says so in one plain sentence with the unlock condition. Correct behavior, not a gap.
- PASS — Q2 (where to market): answerable in seconds — Today's read #2 names Dive Club as the primary home, and the distribution/promotion insights carry exact, verified numbers.
- WARN — Q3 (latest vs the rest): the hero and standings show E6 at 2,133 lifetime views against episodes 25–38 days old with no age cue at glance. Same-age pace is rightly withheld (2 peers), but nothing tells a cold reader the short bar means "4 days old," not "flop."

## Verdict
This is an honest, disciplined shipment. Every number I could re-derive checks out, suppression rules are actually enforced (skeletons instead of thin trends, nothing score-like on young episodes, no fabricated pace), and the mature-vs-same-age honesty language reaches the click layer where the rules demand it. The remaining problems are edges, not rot: one stale-by-a-day insight number, two code paths that read baseline keys this export doesn't carry (harmless today, a silent failure later), a live-trend verdict whose basis isn't the bars beside it, a "helping" bullet sitting on the noise line, and — most consequential for the founders — a default totals view where the biggest bar is an unmarked promo outlier and the smallest bar is simply four days old.

## The one recommendation
Give the newest episode an age cue at glance — a quiet "4 days in — still counting" on the hero metric line and next to E6's standings bar — so lifetime totals stop inviting the wrong read of the one question (how did the latest do?) the dashboard exists to answer.
