# Dashboard critic — 2026-08-22

Model: claude-fable-5 · prompt: critic-prompt.md · artifact: data.json generated 2026-08-22T23:31:25.237Z

---

## 1. Cognitive load
- WARN — Above the fold: hero carries ~9 numbers (1,952 / 949 / 1,003 / 8.2k reach / #3 of 6 / 5% / 4·0·4 chip) and the strip adds ~4 more per card × 6 cards, well past the 12-number cap. The strip doubles as legend/selector, which mitigates, but each card stacks three competing quantities (views, rank badge, pace/status line).

## 2. Readability
- PASS — Rendered copy survives the plain-words test: "typical" not median, "total so far" not cumulative, "not final — settles 08/27" not provisional-freeze jargon. Flagged terms (pillar, ratio, percentile) appear only in code comments and About.
- WARN — reach-conversion recommendation: "turned reach into plays about 2 times better" is a ×-multiple (flagged term) at the click layer, and 41/23 = 1.78, so "about 2 times" also rounds generously. Say "41 vs 23 per 100" and stop.

## 3. Verbosity / suppression
- WARN — Hero verdict shows "Performing about 5% below a typical episode" (|0.9533−1| = 4.67% → rounds to 5%). The contract suppresses differences inside ±5% noise; the code's "typical" threshold is ≤3%, leaving a 3–5% band where noise renders as signal.
- WARN — live-chat insight: "…E6 127 messages in air order. The Mascot Industrial Complex + Live Call-Ins drew 127 from 41 chatters" — the second sentence restates 127 shown one clause earlier; "41 chatters" is its only new content.
- PASS — Compound strip correctly refuses a trend at n=2 clean weeks ("Not enough clean first weeks to call a trend yet") and renders no empty-slot bars.

## 4. Fact-check
- PASS — Re-derived: watch-split 12,710/22,436 = 56.7% → 57% (E1 46%, E3 72% ✓); host-split 18,784+14,148 = 32,932 = Σ reach ex-E3, 57% ✓; reach-conversion E4 1,870/4,532 = 41, E5 2,048/8,900 = 23, sample of 4 ✓; engagement ranks (49.7 high, 30 low, n=5) ✓; live peak 71 = #2 of 6 vs typical 64 ✓; freeze date 08/20+7d = 08/27 ✓; all honesty markers (trackedLate, provisional ~, week1 pending, commentersPer1k note) surface, and no marker appears without its condition.
- WARN — watch-split's "57%" includes E3, which the dashboard itself orders excluded as a promo outlier elsewhere; ex-E3 the X share is 8,504/16,628 = 51%. The two-platform conclusion holds either way, but the headline number is juiced by an episode the reader is told to discount.
- WARN — verify by hand: badge/tooltip partial-mark reads `r.coverage?.missingPillars`, but this export carries `rating.missingPillars` (E6: `["watch"]`). If data.js matches the export's shape, the `*` mark never renders on any badge despite E6's missing watch pillar — check the actual path in data.js.

## 5. Decision usefulness
- PASS — Q1 (compounding): answered in seconds, and honestly — "not enough clean first weeks," with why (E3/E4/E5 late, E6 completes 08/27).
- WARN — Q2 (where to market): top-3 insights carry the calls (X first-class, lead from @ridd_design), but the E3 promo-outlier warning is buried under the "All 10 insights" expander while E3's 5,808 stands as the tallest bar in the default standings chart with no marker on its card or bar.
- PASS — Q3 (latest vs rest): hero badge "#3 of 6 ~" plus a plain one-line verdict answers it at a glance.

## Verdict
The numbers check out — every insight I could re-derive matched the data, and the honesty machinery (tracked-late, provisional, pending-week, missing-count notes) fires everywhere it should, pending one path check on the `*` partial mark. Copy is genuinely plain. The weaknesses are quieter: the above-fold number count is roughly double the budget, a 4.67% difference renders as a 5% verdict inside the noise band, and the dashboard's single most eye-catching number — E3's chart-topping total — carries its "this is promo, not signal" warning only behind an expander. A cold reader answers all three questions, but the one most likely misread number is the least guarded one.

## The one recommendation
Put the promo-outlier flag where E3's number is: a ⚠ mark on E3's strip card and next to its bar total in standings (reuse the existing amber chip/mark pattern), tooltip carrying the anomaly text. The caveat must live on the number it caveats, not three clicks below it.

## Builder triage

- **Fixed:** the announce recommendation no longer uses a multiple; the latest-chat count is no longer repeated; rating verdicts now treat differences through 5% as ordinary noise.
- **Rejected with evidence:** the all-show platform split intentionally includes E3 because it states the share of *all actual watching*. Removing observed watching would make that definition false. Promo outliers remain excluded from host and announce-hook comparisons, where promotion would distort the decision.
- **Rejected with evidence:** the rating-marker warning repeats a field-path false positive already recorded in the v5 ledger. E6 ships `rating.coverage.missingPillars`, the code reads that exact path, and the browser renders `*`.
- **Queued to W11:** the fold count and placing the promo warning beside E3's glance numbers will be judged together in the required owner screenshot pass after W10.
