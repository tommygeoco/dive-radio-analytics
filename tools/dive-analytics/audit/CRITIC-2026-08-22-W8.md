# Dashboard critic — 2026-08-22

Model: claude-fable-5 · prompt: critic-prompt.md · artifact: data.json generated 2026-08-22T23:26:31.729Z

---

## 1. Cognitive load
- WARN — Above-the-fold count blows the 12-number budget: hero carries ~9 (1,952 / 949 / 1,003 / 8.2k reach / #3 of 6 / 5% / 4 enjoyed / 0 didn't / 4 people) and each of six strip cards adds views + "#x of N" + a pace/provisional line (~20 more). The rating badge on every historical card restates rank data already available in the table and tooltip.

## 2. Readability
- PASS — Plain-words discipline holds where it counts: "typical" for median, "total so far" in the race scope line, pillars translated to "how long people watch" / "live turnout"; no banned jargon in glance or click copy.
- WARN — Strip card glance text "provisional — settles 08/27": "provisional" is spec-speak; the tooltip already has the plain version ("Not final — settles 08/27"). Use that.

## 3. Verbosity / suppression
- FAIL — Compound verdict "E2's first week matched E1's (±4%) — week-1 audience is holding flat." is a trend claim from n=2 clean weeks (violates the n<3 rule) on a difference inside the ±5% noise band, and the newest clean week is a month old. The `vals.length < 2` gate should be `< 3`.
- WARN — host-plays-split ("53% ridd / 47% tom") includes E3's 4,206 outlier plays (6,732+5,978=12,710 = all six episodes), while the anomaly caveat claims the outlier is "excluded from host-split aggregates automatically." True for impressions (n=5), false for plays (n=6) — the caveats contradict each other.
- WARN — live-chat insight says "trending down" but the newest delta is up (E5 100 → E6 127, +27%). The sequence is shown honestly; the verdict sentence over-claims direction — "down from launch" is what the data supports.

## 4. Fact-check
- PASS — Re-derived: watch-split 12,710/22,436 = 56.7%→57%, range 46% (1,782/3,854) to 72% (4,206/5,808) ✓; reach-conversion 1,870/4,532=41, 4,206/19,633=21, ≈2× ✓; live-peak #2 of 6 (only E1's 90 > 71), typical 64 = median of priors ✓; anomaly typicals 1,836 and 7,164 are medians ✓; freeze date 08/27 ✓.
- WARN — verify by hand: every `*` partial-basis marker is gated on `r.coverage?.missingPillars`, but the data export carries `missingPillars: ["watch"]` at `rating` top level with no `coverage` object. If data.js matches the export, E6 renders `~` but never `*`, silently dropping a required honesty marker. Check: E6's badge tooltip should say "uses engagement and … only."
- WARN — reach-conversion anchors its comparison on E3, the flagged promo outlier: its 21-per-100 conversion is largely the promo dilution itself, so "E4's hook closed 2× better" conflates hook quality with paid reach. The anomaly insight tells readers not to read E3's numbers as signal; this insight then does exactly that.

## 5. Decision usefulness
- WARN — Q1 (compounding?): a cold reader gets a sentence in 10s, but it answers July (E1 vs E2); with E3–E5 tracked late and E6 pending, the surface cannot currently answer "now."
- PASS — Q2 (marketing effort): yes — watch-split + host-split surface in the top 3 with a concrete announce-machine action.
- PASS — Q3 (latest vs rest): yes — hero badge "#3 of 6" plus one judgment line answers it in one glance.

## Verdict
The arithmetic is clean — every insight number I re-derived checks out, honesty markers for pending/late/missing data are present where the data demands them, and the plain-words discipline is genuinely good. Two real problems remain: the "Is the show growing?" strip makes a flat-trend claim from two month-old data points, which breaks the dashboard's own small-n and noise rules on its most important question; and the `*` partial-basis marker may never render because the code reads a `coverage` object the data doesn't appear to have. The rest is polish: too many numbers above the fold, one contradictory caveat pair, and one insight leaning on the outlier it elsewhere disowns.

## The one recommendation
Fix the compounding strip: raise the trend gate to ≥3 clean weeks so the current n=2 "holding flat" verdict suppresses to "Not enough clean first weeks to call a trend — next clean week completes 08/27 (E6)," since E3–E5's clean weeks will never exist and this surface owns the dashboard's first question.

## Builder triage

- **Fixed:** the first-week verdict now waits for three clean weeks; the current page says there is not enough data and names the late and pending weeks.
- **Fixed:** the strip now says “Not final”; live chat says “down from launch”; host-play and announce-to-watch comparisons leave promo outliers out.
- **Rejected with evidence:** the missing-rating-marker warning read the export incorrectly. E6 ships `rating.coverage.missingPillars = ["watch"]`, and the rendered card shows `#3 of 6 * ~` with the missing-watch explanation.
- **Queued to W11:** the above-fold number count is the named W11 workstream and needs the owner screenshot review before release.
