# Dashboard critic — 2026-08-23

Model: claude-fable-5 · prompt: critic-prompt.md · artifact: data.json generated 2026-08-23T00:57:34.889Z

---

## 1. Cognitive load
- PASS — Above the fold a cold reader faces ~10 numbers (health 52; hero 1,952 + "#3 of 6"; six bar totals on the standings chart), under the 12 cap; the episode strip is collapsed behind "Episodes" and each surface answers one question.
- WARN — The live-chat insight packs seven numbers into one glance sentence ("383 → 310 → 169 → 167 → 100 → 127 ... 41 chatters"). "Down from 383 at launch to 127 latest" carries the same decision with two.

## 2. Readability
- PASS — No hard-flag jargon in glance/click layers: pillar ratios render as "26% below typical", rank as "#3 of 6", reach as "exposure, not watching". "Impressions" and weighting math live only in About, where they belong.

## 3. Verbosity / suppression
- WARN — The pace-rank insight is filtered out of the list on the claim it "lives in the hero verdict," but `buildHero` renders no pace verdict; the "too early to compare — no prior episode has data this young" claim appears nowhere on the page. Low harm (it's a nothing-to-decide note, and the panel's "no same-age comparison yet" partially covers it), but code comment and render disagree.
- WARN — Latent absence-as-zero: the data table and standings tooltip render `totalViews ?? 0` and `xImpressions ?? 0`. Nothing is null today so nothing renders wrong, but the first missing value will print "0" and break the "absence ≠ zero" rule that every other surface honors.

## 4. Fact-check
- PASS — watch-split re-derives exactly: X plays sum 12,710 of 22,436 total views = 56.7% → 57%; per-episode range E1 1,782/3,854 = 46%, E3 4,206/5,808 = 72%.
- PASS — Host aggregates re-derive with the promo outlier correctly excluded: announce impressions 18,784 + 14,148 = 32,932 = Σ xReach minus E3; broadcast plays 5,303 + 3,201 = 8,504 = Σ xPlays minus E3; conversion 41 = E4 1,870/4,532, 23 = E5 2,048/8,900.
- PASS — All five rating scores re-derive from pillar ratios × weights (e.g., E6: 0.812×0.4 + 1.191×0.3 + 0.904×0.3 = 0.9533); E6's missing watch pillar redistributes weight 0.5→{0.4/0.3/0.3}, and the ~/partial state surfaces both on the badge tooltip and the hero "Rating is still settling; one check is not in yet." line.
- PASS — Health: score 52 within 8 of weightedMean 53.3 (re-derived 53.33 from subscores × effective weights); null checks (growth, commentRate) carry reasons and zero weight; each bullet carries exactly one number matching a fact display (71, 12.9%, 1.6, 127) with the required "still early" phrases; count=1 → no trend rendered, correct.
- WARN — Anomaly insight's "19,633 X reach vs a typical 7,164": I cannot re-derive 7,164 from current values (median of non-outlier ≥7d episodes' reach = 5,643; the plays typical of 1,836 does re-derive). The >2× outlier conclusion holds under any candidate typical, so the classification stands — verify by hand: where the frozen anomaly text's reach typical came from.
- WARN — "The latest show had 41 chatters" cites a field (`live.chatters`) absent from the export provided; verify by hand that 41 matches the stored chatters count.

## 5. Decision usefulness
- PASS — Q1 (compounding): yes — glance layer honestly says "Growth trend isn't ready yet" with a 2-of-3 readiness dot; the correct answer right now is "can't know yet," delivered in one sentence.
- PASS — Q2 (where to market): yes — top-3 insights surface the two decisions that matter (57% of watching is on X; ridd's account travels further) with recommendations one click away.
- PASS — Q3 (latest vs rest): yes — hero gives 1,952 total, the YT/X split bar, "#3 of 6," and the settling caveat within one glance.

## Verdict
This is a disciplined, honest artifact. Every headline percentage, host aggregate, rating score, and health number I could re-derive checked out exactly, outlier exclusions are applied where the rules say and not where they don't, and missing data says so in plain words on every visible surface. The remaining issues are small: one insight sentence is overloaded with numbers, one frozen anomaly "typical" can't be re-derived from current data (though its conclusion survives), a code comment promises a hero pace verdict that doesn't exist, and two low-traffic surfaces (table, standings tooltip) would print zero for a missing value if the data ever goes gappy.

## The one recommendation
Replace the `?? 0` fallbacks in the data table and standings tooltip (`totalViews ?? 0`, `xImpressions ?? 0`, and the per-channel `?? 0` cells) with the same "not available" / "–" treatment the hero and strip already use — it's the only place a constitutional rule is one missing value away from being broken.

## Builder triage

- **Fixed:** table and tooltip missing values now say “not available” or “–”; a real stored zero remains visible. The validator rejects a missing-to-zero fallback.
- **Fixed:** live-chat copy now compares only the first and latest stored message totals. The full history and chatter count remain in the Live table.
- **Fixed:** the non-actionable waiting pace insight is suppressed. Same-age pace appears in the panel, insight list, Slack, and alerts only after three real peers exist.
- **Rejected with evidence:** E3's 7,164 reach baseline is correct. All six stored reach values sort to 4,532 / 5,172 / 6,114 / 8,214 / 8,900 / 19,633, so the middle is (6,114 + 8,214) / 2 = 7,164. The critic prompt now names that all-episode rule.
- **Rejected with evidence; critic bundle fixed:** E6 has 41 stored chatters in its Restream event and `data.json`. The compact critic bundle omitted the field; it now includes `live.chatters`.
