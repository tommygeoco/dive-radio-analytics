# Dashboard critic — 2026-08-22

Model: claude-fable-5 · prompt: critic-prompt.md · artifact: data.json generated 2026-08-22T23:51:57.467Z

---

## 1. Cognitive load
- WARN — Above-fold budget is over 12 before the strip: the health card carries 5 numbers (52, 71, 12.9%, 1.6, 127), the hero adds ~6 (1,952 / 949 / 1,003 / 8.2k reach / "#3 of 6" / pace chip), and the sentiment chip adds 3 more. The strip below adds ~18 further. Contract says 12.
- PASS — One question per surface holds: health = overall state, compound strip = "is it growing?", hero = latest episode, insights = where to push. No surface double-books.

## 2. Readability
- WARN — Four honesty glyphs coexist (◐, →date, *, ~), each with one meaning and About coverage, but a cold reader meets * and ~ on the hero badge with no inline expansion until hover; the tooltip mitigates but doesn't exist for readers who never hover. Otherwise the copy is clean — "typical," "total so far," no flagged jargon at glance or click layers.

## 3. Verbosity / suppression
- WARN — The watch-split headline "57% of all actual watching happens on X (12,710 of 22,436)" includes E3, which the anomaly insight itself says to "treat as promo-driven outlier, not topic signal" and which is excluded from every other aggregate. Without E3 it's 8,504 of 16,628 = 51%. The claim survives either way, but the shipped number is inflated by data the page tells the reader to discount — inconsistent suppression.
- PASS — Small-n honesty is applied correctly: the compound strip suppresses the trend at 2 clean weeks (no bar parade), E6's engagement is excluded from the <7-day comparisons, and the n=1 comment-rate check is nulled with a reason.

## 4. Fact-check
- PASS — Core arithmetic re-derives clean: total views 22,436 ✓; X plays 12,710 (57%) ✓; host plays 5,303/8,504 = 62% ✓; announce reach 18,784/32,932 = 57% ✓; E4 conversion 1,870/4,532 = 41 and E5 2,048/8,900 = 23 ✓; live peak 71 = #2 of 6 with the E2 tie handled correctly ✓; chat sequence 383→310→169→167→100→127 ✓.
- PASS — Ratings recompute exactly with renormalized weights: E6 = 0.812×0.4 + 1.191×0.3 + 0.904×0.3 = 0.9533 ✓ (likewise E2 0.9679, E4 0.9337, E5 0.8159, E3 0.777). Health: score 52 vs weightedMean 53.3 is within 8; no trend at count=1; each bullet carries one number matching its fact's display; both "still early" required phrases surface.
- WARN — Verify by hand: `ratingBadge`, `ratingTipHTML`, and `buildPanel` all read `r.coverage?.missingPillars`, but the ratings export carries `missingPillars` at the rating's top level (`"missingPillars": ["watch"]`, no `coverage` key). If data.js matches the export, E6's `*` partial-basis marker and its "rated on engagement/retention/live only" note never render — a partial condition without its marker. The export is compact (e.g., `live.chat` vs code's `live.chatMessages`), so confirm the real structure before fixing.
- WARN — Verify by hand: the anomaly insight's two "typical" values come from different peer sets — 1,836 plays is the median of {E1,E2,E4,E5}, but 7,164 reach is only reachable as a median of a set excluding E1 or E4. The >2× conclusion holds under any reasonable window, but the mixed baselines should be one window.

## 5. Decision usefulness
- PASS — Q1 (compounding): answered honestly in one sentence — "Not enough clean first weeks to call a trend yet," with when the missing weeks complete. "Unknown" stated plainly is a valid glance answer.
- PASS — Q2 (marketing effort): top-3 insights surface watch-split and host-split first; a cold reader gets "X matters as much as YouTube, lead from @ridd_design" in under 10 seconds.
- PASS — Q3 (latest episode): hero gives total, "#3 of 6," and "right at the show's typical level — not final until 08/27" in one glance.

## Verdict
This is an honest, well-suppressed dashboard: every sum and score I re-derived checks out, missing data says so instead of showing zeros, small samples are named, and all three founding questions are answerable at a glance. Two things keep it from clean: the above-fold number count runs past budget once health, hero, and chips stack up, and the flagship "57% of watching is on X" quietly includes the very episode the page tells readers to discount. The one genuine honesty risk is unverifiable from the compact export — whether E6's partial-basis marker actually renders given the `coverage.missingPillars` path the code reads.

## The one recommendation
Verify (and fix if real) the `r.coverage?.missingPillars` path against the actual data.js structure — if it reads empty, E6's headline "#3 of 6 ~" badge silently hides that the rating is missing its heaviest pillar (views, 50% weight), which is exactly the kind of unmarked partial the constitution forbids.

## Builder triage

- **Queued to W11:** the above-fold count and the glance treatment of rating marks belong in the required owner screenshot pass. The saved health card itself stays one score; its bullets carry grounded evidence.
- **Rejected with evidence:** the all-show X share intentionally counts every observed watch, including the promoted episode. Removing it would make “all actual watching” false. Promo outliers are left out of host and announce-hook comparisons, where promotion would distort the decision.
- **Rejected with evidence:** E6 ships `rating.coverage.missingPillars = ["watch"]`; the browser renders `#3 of 6 * ~` and the missing-watch explanation. The critic harvest had flattened that nested field and created the false warning; the harvest now preserves the real `coverage` object.
- **Rejected with evidence:** both anomaly baselines use the same six-episode set. X plays sort to 1,003 / 1,782 / 1,801 / 1,870 / 2,048 / 4,206, giving 1,835.5 → 1,836; X reach sorts to 4,532 / 5,172 / 6,114 / 8,214 / 8,900 / 19,633, giving 7,164.
