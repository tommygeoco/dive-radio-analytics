# Dashboard critic — 2026-08-22

Model: claude-fable-5 · prompt: critic-prompt.md · artifact: data.json generated 2026-08-22T22:01:10.944Z

---

## 1. Cognitive load
- FAIL — Above the fold a cold reader faces ~30+ numbers against the 12-number cap: the hero alone carries "1,952", "949", "1,003", "8.2k reach", "#3 of 6", the same "#3 of 6" again in the verdict, and a "4 pos · 0 neg · 4 comments" chip (~12), before the six strip cards add 2–3 numbers each and the compounding strip adds more.

## 2. Readability
- FAIL — Glance-layer jargon: the anomaly insight leads with ">2x the all-episode median (4,206 X plays vs 1,870 median…)" — both ×-multiples and "median" are hard flags; "more than double what a typical episode gets" works. Strip cards also say "▼X% vs median" at glance where "vs typical" works.

## 3. Verbosity / suppression
- FAIL — Two insights are n=2 claims dressed as trends: "Episodes stop growing after about 3 weeks" (caveat: "only 2 full-history episodes") and "91% of X reach and 87% of YouTube views arrive in the first 7 days" (caveat: "sample of 2"). The contract says suppress n<3 claims; a caveat admitting the violation doesn't cure it.
- WARN — The hero verdict "Ranks #3 of 6 against the episodes up to its air date…" restates the "#3 of 6" badge rendered immediately beside the hero number — a restated-number violation the code's own comment claims was fixed.

## 4. Fact-check
- FAIL — The "* partial basis" marker can never render: the code reads `r.coverage?.missingPillars` everywhere (badge, hero verdict, tooltip, panel), but the data ships `rating.missingPillars` at the top level. E6 has `missingPillars: ["watch"]` and would display "#3 of 6 ~" with no "*" — a rating condition present in data with its honesty marker suppressed.
- FAIL — E5's rank contradicts the displayed scores: E5 shows "#5 of 5" with score 0.8159, but E3 — inside E5's comparison window — shows score 0.777. Only E1, E2 (0.9679), and E4 (0.9337) beat E5, so it should read #4 of 5; either the rank or the scores shown are wrong.
- FAIL — The partial-history insight caveat says late episodes are "Marked \"late reg\" wherever they appear," but the card flag was removed (per the code's own owner-call comment), the panel chip says "tracked late," and only About says "late reg." The caveat describes a marker that no longer exists, in a term the UI doesn't use.
- WARN — The anomaly text shows "1,870 median" for X plays, but the true median of {1003, 1782, 1801, 1870, 2048, 4206} is 1,836 (the code takes the upper-middle element). The >2× claim survives either convention (4,206 > 3,740 and > 3,671), so this is a convention slip, not a false claim.
- PASS — All other arithmetic re-derives: 12,710/22,436 = 57% X watch share; 18,784/32,932 = 57% announce share with E3 correctly excluded; E4 41/100 vs E3 21/100 conversion; weighted rating scores (e.g., E4: 0.967×.5 + 0.919×.2 + 0.957×.15 + 0.819×.15 = 0.9337) all check out.

## 5. Decision usefulness
- WARN — Q1 (compounding): answerable in 10s ("E2's first week matched E1's — holding flat"), but the answer rests entirely on the two oldest episodes; E3–E5 are excluded and E6 pending, so "is the show compounding" is honestly answered with month-old data — the strip says so, which saves it from a FAIL.
- PASS — Q2 (marketing effort): yes — the top insights deliver directly actionable glance answers (spend promo inside 3 weeks, lead announces from @ridd_design, reuse E4's hook).
- PASS — Q3 (latest vs rest): yes — hero gives "#3 of 6 (provisional)" plus live turnout "#2 of 6 vs typical peak 64" within seconds, though its trustworthiness depends on the lens-4 rating fixes.

## Verdict
The arithmetic in the insight sentences is genuinely solid — every percentage and sum I could re-derive checks out — and the three-layer structure mostly works. But the flagship feature, the rating, is broken in two ways that matter: the partial-basis marker for E6 can never render because the code reads a field the data doesn't have, and E5's shipped rank contradicts the shipped scores. On top of that, the page blows the 12-number above-fold budget by roughly 2–3×, and two headline insights are two-episode samples presented as trends. Ship after the rating fixes; the rest is trimming.

## The one recommendation
Make the rating trustworthy end to end: fix the marker path (`rating.missingPillars`, not `rating.coverage.missingPillars`) so E6's "*" partial-basis flag actually renders everywhere, and re-derive E5's rank so #5-of-5 stops contradicting its 0.816 score — the headline feature must not be the least honest surface on the page.
