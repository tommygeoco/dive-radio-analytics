# LinkedIn without invented viewer coverage

Decision: register Ridd's LinkedIn destination by exact shared YouTube/X broadcast identity from Restream, pinned to the verified existing Restream channel 17403676. Keep this source in `show.linkedin` so an unsupported platform cannot enter the YouTube/X snapshot transport and break its required cohort.

Restream's LinkedIn viewer zeros are unsupported fields, projected as null. Its actual message and chatter counts remain usable, already included once in session chat totals. Historical scores and their existing source cohorts remain unchanged. LinkedIn owner observations are shown separately and explicitly dated; their post impressions never enter video views, their post comments never sum with overlapping live chat, and they do not imply prior-day history.

A private owner import inbox provides an interim path. Exact post identity, nonnegative numeric metrics, ordered observations and file hashes ground the public projection. No secrets, raw exports, or private file paths are published. API approval/OAuth remains an external dependency; no untested automatic collector is advertised as working.

Consequences: LinkedIn links, chat, feedback, source status and recommendation facts ship now. Automatic viewing analytics, replay comments and cross-platform viewing comparisons await actual access and source-specific comparability proof. The existing archive already classifies LinkedIn chat; the historical Restream-only mode fills older episode text without querying X beyond its recent-search window.
